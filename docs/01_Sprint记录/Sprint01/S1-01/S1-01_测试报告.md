# S1-01 实施测试报告

**Issue**：`S1-01`
**阶段**：`implementation`
**日期**：2026-09-12
**目标**：`sprint01` / `D:/Tony/Documents/invest2026/projects/viber-coding/dsh-plugin-codex`

## 1. 绑定环境

| 项目 | 结果 |
|:-----|:-----|
| Node | `v24.14.0` |
| pnpm | `11.7.0` |
| Codex CLI | `0.154.0`；`codex login status` 为 `Logged in using ChatGPT` |
| DSH 上游 | `D:/Tony/Documents/invest2026/projects/viber-coding/deepseek-harness`，`dsh-v0.1.5-rc.1`，`183f08e9c6dde7e36cd2318eaee70b0da08fb35e` |
| 当前项目依赖来源 | `pnpm-lock.yaml` 的 DSH 本地 link 指向上述上游；Codex exact `0.154.0` |

上游工作树没有 tracked 修改，只有用户已有的 `docs/05_预研/` 未跟踪文档；本阶段没有修改、暂存或删除它们。

## 2. 命令证据

| 顺序 | 命令 | 退出码 | 直接结果 |
|:-----|:-----|:-----:|:---------|
| 1 | 上游 `pnpm.cmd install --frozen-lockfile` | 0 | rc1 依赖安装完成 |
| 2 | 上游 `pnpm.cmd run build:lib:host` | 1 | 上游现有 root `tsdown.config.ts` 将 `lib/types/{index,invariant,startup}.js` 作为未解析入口；错误为 `Cannot find entry` |
| 3 | 当前项目 `pnpm.cmd install --frozen-lockfile` | 0 | `Already up to date` |
| 4 | `node.exe node_modules/typescript/bin/tsc --noEmit` | 0 | TypeScript 类型检查通过 |
| 5 | `pnpm.cmd test` | 0 | `12` 个测试文件、`137` 个测试通过 |
| 6 | `pnpm.cmd run build` | 0 | host/client 产物构建通过 |
| 7 | `pnpm.cmd run package:verify` | 0 | tarball 白名单校验通过，`39` 个文件 |
| 8 | `pnpm.cmd run check` | 0 | release verify、测试、构建、打包校验和 pack 全部通过 |
| 9 | 有界 Codex probe | 0 | `initialize` + `model/list(includeHidden=false, limit=100)` 通过；输出不含凭据 |
| 10 | `pnpm.cmd run test:e2e`（继承当前用户 `CODEX_HOME`） | 1 | Codex `0.154.0` 在 `initialize` 前因旧 `network_access` 配置键关闭协议流；见 `C:/Users/Tony/.codex/config.toml:6` |
| 11 | 隔离 E2E wrapper（`D:/temp` 临时 home、复制已登录 `auth.json`、空 `config.toml`，随后清理） | 0 | 既有 `tests/real-codex.e2e.ts`：`1` 个测试通过，耗时约 `13.94s` |

上游 `build:lib:host` 的失败属于父仓库聚合入口问题，不是本项目源代码的构建结果。本项目所需的 linked package 产物在上游工作树中完成生成并以外部 `koffi` 依赖方式重建，避免原生模块被错误内联；这些生成文件被忽略，未改变上游源代码或用户文档。因此本报告把父仓库聚合失败保留为 legacy，不把它合并为插件构建通过。

## 3. Codex 目录探测

探测脚本使用当前本地 `@openai/codex` 包、临时空配置和本机登录状态，只打印已脱敏的 `userAgent` 与目录字段：

```text
userAgent = s1-01-model-probe/0.154.0 (Windows 10.0.19045; x86_64) WindowsTerminal (s1-01-probe; 0.1.0)
ids = gpt-6-astra, gpt-5.6-sol, gpt-5.6-terra, gpt-5.6-luna, gpt-5.5, gpt-5.3-codex-spark
inputModalities = text+image for the first five; text for gpt-5.3-codex-spark
reasoning = Astra/Sol/Terra/Luna/5.5 as recorded in the catalog; Spark default high
```

`model/list` 本身没有 `contextWindow` 字段；只读的当前 Codex metadata cache 为前五个模型 `272000`、Spark `128000`，因此静态目录和文档只同步这些可追溯部署元数据。这里的 context 数值是本地 dated metadata provenance，不是本次 App Server 响应字段，也不被当作运行时目录能力断言。`webSearchModel` 示例按本阶段用户指示为 `gpt-5.6-luna`。

## 4. E2E 观察范围

隔离 E2E 通过既有测试断言了：一次动态工具 callback、同一 Session 的暖续接、缓存续接、持久图片输入、图片内容不进入重放，以及单一 subprocess 在 dispose 后 `waitForExit()` 成功。默认用户配置失败没有被隐藏，已转化为 README 的 `--strict-config` 迁移说明。

## 5. 未执行与遗留

- 没有运行远程 CI、发布、推送、合并或用户验收。
- 上游 root `build:lib:host` 的 brace-entry 错误和其 client 构建中的 `RemoteError` 导出错误仍由上游 owner 处理；本 Issue 不修改父仓库源代码。
- 使用旧 Codex 配置的直接 `pnpm test:e2e` 仍需先迁移 `CODEX_HOME`；兼容 home 下的真实 App Server 烟测已通过。
