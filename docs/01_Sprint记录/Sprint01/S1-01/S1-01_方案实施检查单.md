# S1-01 方案实施检查单

**输入**：`S1-01-SD-v1`、`S1-01-DP-v1`、`S1-01-IP-v2` / 唯一单元 `P01`
**目标**：`sprint01` / `D:/Tony/Documents/invest2026/projects/viber-coding/dsh-plugin-codex`
**实施报告**：[S1-01 测试报告](S1-01_测试报告.md)

## 1. 验收标准

| AC | 结果 | 证据 |
|:---|:-----|:-----|
| AC-1 版本图 | pass | `package.json`、`pnpm-workspace.yaml`、`pnpm-lock.yaml`、Runner、README 和 workflow 已统一到 DSH `0.1.5-rc.1` / Codex `0.154.0`；历史方案和测试中的旧字符串仅保留其历史或任意 override 语义 |
| AC-2 锁和 CI | pass with upstream legacy | 当前项目 `pnpm install --frozen-lockfile` 退出 0，两个 workflow 与 README 指向同一 rc1 commit；上游 root 聚合 build 的既有 brace-entry 错误记录在测试报告，不改变本项目锁和 CI 指针。该错误是父仓库遗留，不被改写为通过 |
| AC-3 运行时身份 | pass | `src/runner.ts` 严格检查 `initialize.userAgent` 包含 `0.154.0`；Runner 单元通过；真实 probe user-agent 含 `0.154.0` |
| AC-4 DSH Interface | pass | `node node_modules/typescript/bin/tsc --noEmit`、`pnpm run build` 通过；`tests/runner.spec.ts` mock 已移除 rc1 不再提供的 `pid` |
| AC-5 既有行为 | pass | `pnpm test`：`12/12` files、`137/137` tests；覆盖 runner、wire、adapter、protocol、Session、图片、Web Search 和 client surface |
| AC-6 目录真实性 | pass with metadata provenance | Codex `0.154.0` 有界 probe 返回六个可见模型；静态目录、设置卡片、Cordis patch、测试与双语文档同步 ID、模态、推理选项。`contextWindow` 不在 `model/list` 响应中，272000/128000 仅来自只读本地 Codex metadata cache，静态字段不冒充运行时响应 |
| AC-7 本地 App Server | pass under compatible logged-in home | 隔离 `CODEX_HOME` 的既有 `tests/real-codex.e2e.ts` 通过 `1/1`；工具 callback、暖续接、图片输入和进程树清理均有测试断言。继承旧 `network_access` 配置的直接命令失败已如实记录 |
| AC-8 发布包 | pass | `pnpm run package:verify` 与 `pnpm run check` 退出 0；既有 tarball 白名单验证 `39` 个文件 |

`related_regression_gate=pass`：项目自身的类型、完整单测、构建、打包和兼容 home E2E 均通过。上游聚合 build 失败是独立 legacy，不由本项目代码修复或隐藏。

## 2. 范围与工艺核对

- `P01` 的版本图、Runner 身份、目录证据、CI/文档和验证作为一个串行实施单元完成，没有新增 Adapter、Seam、registry、动态目录服务或发布流程。
- 真实目录证据直接命中了 `src/client/CodexSettingsCard.tsx`、`cordis.patch.yml`、`tests/plugin.spec.ts` 和 `tests/settings-card.spec.ts`；它们是批准方案 §4.4 允许的“受直接 Interface/目录证据影响的既有文件”，并已在 `S1-01-IP-v2` 补录到 P01 owned files，与 `src/index.ts` 保持同一模型 owner。阶段报告文件是实施交接表面，不是生产逻辑。
- `CodexAppServerAdapter`、`CodexAppServerRunner` 的接口和资源生命周期 owner 未移动；`koffi` 只在上游忽略生成物中外部化，当前项目没有复制上游源代码。
- `codebase-design`：现有 `LlmAdapter`、`SubprocessHandle` 和 `assistant-step` seam 足够承载差量，结构结果为 pass。
- `code-craft(mode=apply)`：参数 owner、错误语义、依赖方向和 TypeScript/ESM 表达保持既有惯例，结果为 pass。
- `no-negative-echo`：报告只保留已接受事实、当前失败签名、遗留 owner 和恢复动作，没有把失败或未执行范围表述为通过。

## 3. 交接判定

| 项目 | 结果 |
|:-----|:-----|
| 保真 | pass：目标版本、批准方案和 AC 均有实际文件或命令证据 |
| 最小 | pass：复用既有依赖图、Module、Interface、测试和静态目录；无新增机制 |
| 闭环 | pass：安装、类型、单测、构建、目录、E2E、打包和遗留均有定位 |
| 边界 | pass：父仓库源代码、用户未跟踪文档、远程 Git、发布和验收均未修改 |
| `stage_check` | partial_with_legacy |
| `next_stage` | acceptance（仅工程交接，等待用户验收；上游聚合构建和默认旧配置遗留仍保留） |
