# S1-01 [TASK] dsh-plugin-codex 依赖升级到 DSH 0.1.5-rc1 并支持 Codex 0.154

## 登记信息

- 状态：待办
- 优先级：未提供
- 估算：未提供
- 角色：未提供
- 显式依赖：未提供
- 父子关系：无；S1-01 是新的顶层原子 Issue
- 关联 Issue：未提供；与其他 Sprint 事项的关系不作推断
- 目标仓库：用户 fork 的 `dsh-plugin-codex`；目标 Sprint 为 `Sprint01`，当前目标分支为 `sprint01`
- 来源：当前会话用户消息；用户确认 `1. 确认登记`；确认对象 `S1-01-issue-register-v1`

## 原始需求

在用户 fork 的 `dsh-plugin-codex` 目标仓库的 Sprint01 中登记一个依赖升级任务，目标为 DSH `0.1.5-rc1` 并支持 Codex `0.154`。

## 已提供事实

1. 用户要求登记 DSH `0.1.5-rc1` 的依赖升级目标。
2. 用户要求 Codex 支持 `0.154`。
3. 用户明确说明目标仓库是自己的 fork，可以修改。
4. 用户要求直接在目标仓库的 Sprint1 中登记 Issue。

## 原子边界

本 Issue 只登记一个原子目标：记录 `dsh-plugin-codex` 的 DSH `0.1.5-rc1` 与 Codex `0.154` 依赖升级目标。

不包含方案决策、架构或 ADR、代码与依赖文件修改、安装、兼容性验证、测试、用户验收、远程 GitHub Issue 创建、推送或合并。

## 初步验收意图

- [ ] Sprint01 跟踪表登记 `S1-01`，状态为 `待办`。
- [ ] 本原始需求文件记录 DSH `0.1.5-rc1` 与 Codex `0.154` 两个用户指定目标。
- [ ] 实际依赖修改、兼容性结论、测试和验收标准留待后续阶段确定；登记阶段未提供。

## 歧义与未知

- Codex `0.154` 对应的精确 patch 版本、包名和 lockfile 目标：未提供。
- 当前依赖版本、依赖字段、验证环境和实施复杂度：未提供。
- 负责人、优先级和估算点数：未提供。
- “Sprint1”与具体文档/分支命名：已按目标仓库现有 `Sprint01` 跟踪表和 `sprint01` 分支定位；若用户指向其他目标，需重新确认。

## 估算依据

未提供；登记不推断实施复杂度或点数。

## 用户原话

```text
[$issue-register](D:\Tony\ubuntu_settings\\.claude\skills\issue-register\SKILL.md) 登记依赖升级到dsh 0.1.5-rc1
```

```text
codex需要支持 0.154
```

```text
外部仓库是我的fork仓库，可以修改
```

```text
你直接在目标仓库登记issue，目标仓库是sprint1
```

## 保真核对

- 草案版本：`S1-01-issue-register-v1`
- 输入定位：当前方案决策会话中的上述用户消息边界
- 未更改原始需求：`pass`
- 原话原本记录：`pass`
- 差异：未发现；派生的仓库、Sprint 和分支定位未冒充用户原话

## 登记确认

- 确认输入定位：当前会话的登记确认帧
- 用户原话：`1. 确认登记`
- 确认对象：`S1-01-issue-register-v1`
- 授权范围：写入本原始需求文件、Sprint01 跟踪行、精确登记提交和结果回读
- 未授权范围：方案决策、设计、实施、依赖升级、兼容性验证、测试、用户验收、远程 GitHub Issue 创建、推送或合并
