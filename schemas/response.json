{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "DeepSeek Harness Codex bridge response",
  "type": "object",
  "properties": {
    "kind": {
      "type": "string",
      "enum": ["message", "tool_calls"],
      "description": "Use message for a final assistant reply, or tool_calls to request Harness tools."
    },
    "text": {
      "type": "string",
      "description": "The non-empty final reply when kind is message; otherwise an empty string."
    },
    "calls": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "minLength": 1,
            "description": "An exact tool name from the Harness request."
          },
          "arguments": {
            "type": "string",
            "description": "One JSON object serialized as a string and valid against that tool's parameters schema."
          }
        },
        "required": ["name", "arguments"],
        "additionalProperties": false
      }
    }
  },
  "required": ["kind", "text", "calls"],
  "additionalProperties": false
}
